import { PrintScreenDirective } from './print-screen.directive';

describe('PrintScreenDirective', () => {
  it('should create an instance', () => {
    const directive = new PrintScreenDirective();
    expect(directive).toBeTruthy();
  });
});
